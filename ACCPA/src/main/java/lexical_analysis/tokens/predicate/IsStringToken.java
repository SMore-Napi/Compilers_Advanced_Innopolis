package lexical_analysis.tokens.predicate;

public class IsStringToken extends PredicateToken {
    public IsStringToken(int row, int column, String content) {
        super(row, column, content);
    }
}
