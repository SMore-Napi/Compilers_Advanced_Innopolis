package lexical_analysis.tokens.base_type;

import lexical_analysis.tokens.Token;

public class BaseTypeToken extends Token {
    public BaseTypeToken(int row, int column, String content) {
        super(row, column, content);
    }
}
